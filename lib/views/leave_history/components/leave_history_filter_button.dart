
import 'package:flutter/material.dart';


class LeaveHistoryFilterButton extends StatelessWidget {
  const LeaveHistoryFilterButton({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Leave history button'),
      ),
    );
  }
}
